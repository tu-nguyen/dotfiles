# ~/.bash_extras/.bash_motd

#------------------------------------------
# Display a message of the day (MOTD) when starting a new terminal session
#-------------------------------------------
motd() {
  date
}